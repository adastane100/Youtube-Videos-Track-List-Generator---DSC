apiVersion: apps/v1
kind: Deployment
metadata:
  name: segmentation-worker
spec:
  selector:
    matchLabels:
      app: segmentation-worker
  replicas: 1
  template:
    metadata:
      labels:
        app: segmentation-worker
    spec:
      containers:
      - name: segmentation-worker
        image: kaidrumm/segmentation-worker:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 100m
            memory: 4Gi
        env:
        - name: REDIS_HOST
          value: "redis"
        - name: REDIS_PORT
          value: "6379"
        - name: MINIO_HOST
          value: "minio:9000"
        ports:
        - containerPort: 5002